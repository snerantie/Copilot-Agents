{
  "name": "manco-admin-agent",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Copilot Extension (admin agent) that logs tickets into the Jira MANCO project.",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/jira.js && node --check src/copilot.js && node --check src/verifySignature.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
